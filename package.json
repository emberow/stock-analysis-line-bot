{
  "name": "echo-bot",
  "version": "0.0.0",
  "description": "An example LINE bot just to echo messages",
  "main": "index.js",
  "scripts": {
    "start": "start cmd /k node . && start cmd /k ngrok http 3000"
  },
  "dependencies": {
    "@line/bot-sdk": "^7.3.0",
    "express": "^4.16.3"
  }
}
